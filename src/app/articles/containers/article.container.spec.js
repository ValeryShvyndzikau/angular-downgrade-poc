define(function(require) {
  var articleContainer = require("./article.container");

  describe("ArticleContainer", () => {
    it("Should work", () => {
      expect(1).toEqual(1);
    });
  });
});
